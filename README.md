# React Starter

Minimal **React + Vite** project scaffold so we can build together from scratch.

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

# V65 Website

This is a minimal React + Vite project that recreates the editorial landing page styling (mobile + desktop).

## Run locally

```bash
npm install
npm run dev
```

Then open the dev server URL shown in your terminal (usually `http://localhost:5173`).


